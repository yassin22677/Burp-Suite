# Copy to .env and adjust (app loads .env from project root).
# PostgreSQL — must exist before first run (create DB + run SQL scripts in scripts/ if needed).

DATABASE_URL=postgresql://postgres:123@localhost:5432/burp_rl

# Flask session signing (set any random string in production)
SECRET_KEY=change-me-in-prod
